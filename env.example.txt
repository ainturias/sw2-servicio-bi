# Variables de entorno para el Microservicio de Business Intelligence
# Copia este archivo a .env y completa con tus valores reales
# O renombra este archivo a .env y edítalo

# ============================================================================
# PostgreSQL (Base de datos analítica)
# ============================================================================
PG_DATABASE=agencia-viajes-bi
PG_USER=postgres
PG_PASSWORD=7550
PG_HOST=localhost
PG_PORT=5432
# Para desarrollo local: disable
# Para producción (Neon/Supabase/ElephantSQL/Render): require
PG_SSLMODE=disable

# ============================================================================
# MongoDB Atlas (Core transaccional - del compañero)
# ============================================================================
# URI de conexión completa de MongoDB Atlas
# Ejemplo: mongodb+srv://usuario:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_DATABASE=agencia-viajes

