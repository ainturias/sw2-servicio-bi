### Ejemplo de variables de entorno para ejecutar localmente con docker-compose
# Copia este archivo a `.env` y ajusta los valores antes de ejecutar `docker-compose up`

# PostgreSQL (contenedor postgres en docker-compose)
PG_DATABASE=agencia-viajes-bi
PG_USER=postgres
PG_PASSWORD=7550
PG_HOST=postgres
PG_PORT=5432
PG_SSLMODE=disable

# MongoDB (si usas el servicio `mongo` del docker-compose, deja la URI como abajo)
MONGO_URI=mongodb://mongo:27017
MONGO_DATABASE=agencia_viajes

# Variables para el proxy / token del BI (solo ejemplo, cambia para producción)
BI_AUTH_TOKEN=changeme
BI_BASE_URL=http://localhost:8000

# Opcional: si conectas a una DB PostgreSQL externa (ej. Supabase), reemplaza con los valores reales
# PG_HOST=aws-1-us-east-2.pooler.supabase.com
# PG_SSLMODE=require
